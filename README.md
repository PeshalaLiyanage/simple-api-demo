# simple-api-demo
This repository is created to demonstrate a simple API

# Run the app
- Run `npm i`
- Run `npm start`

# Create a Express js app from scratch

- Run `node init -y`
- Run `npm i express`
- Run `npm i nodemon` 